# Fix Minecraft color code processing in instructions popup

## Problem
The instructions popup strips Minecraft color codes (§0-§f, §l, etc.) using regex `.replace(/§[0-9a-fk-or]/g, '')`, resulting in plain text without colors/formatting.

## Solution
- Preserve color/format codes in the instruction text
- Convert codes to HTML tags (colors via inline styles, formatting via semantic tags)
- Use `innerHTML` to render styled content
- Ensure X button remains functional (already separated)

## Implementation

1. **Update name parsing in `formatInstructions`** (index.html)
   - Remove `.replace(/§[0-9a-fk-or]/g, '')` from the `name` assignment in both places:
     - Inside the slot loop (line ~366)
     - Inside the copyCommands loop (line ~376)
   - Keep other replacements: `_` to space, `ENCHANTED` to `E.`

2. **Add `convertMinecraftCodes` function** (index.html, after `formatInstructions`)
   - Map color codes 0-f to hex colors (standard Minecraft palette)
   - Map formatting codes: l→`<strong>`, o→`<em>`, n→`<u>`, m→`<del>`, k→`<span class="obfuscated">` (optional)
   - Implement parser: iterate string, on `§`+code push appropriate tag onto stack, on `§r` close all tags, normal characters appended (HTML-escaped)
   - Return HTML string

3. **Modify `formatInstructions` to return HTML**
   - Build the plain text result as before (with color codes intact)
   - Return `convertMinecraftCodes(result)`

4. **Update `showInstructions`**
   - Change `document.getElementById('craft-popup-content').textContent = formatInstructions(data);`
   - To: `document.getElementById('craft-popup-content').innerHTML = formatInstructions(data);`

5. **Optional CSS for obfuscated** (if implemented)
   - Add keyframes to animate character alternation; can skip for brevity.

## Files to Modify
- `/Users/rithwiksaxena/Code/hypixel-bazaar-flipper/index.html`

## Testing Notes
- Verify color codes appear with correct colors in popup
- Verify bold/italic/underline/strikethrough render (if implemented)
- Verify X button still closes popup
- Check multiple consecutive colour changes work
